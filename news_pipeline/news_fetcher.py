

SLEEP_TIME_IN_SECONDS = 5

def handle_message(msg):
    if msg is None or not isinstance(msg,dict):
        print 'message is broken'
        return

    task = msg
    text = None

    if test['source'] == 'cnn':
        print 'scraping CNN news'
        text =

while True:
    if scrape_news_queue_client is not None:
        msg = scrape_news_queue.

